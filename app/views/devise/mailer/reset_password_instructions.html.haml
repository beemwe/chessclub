%p= t('.salutation', :user => @resource.username)
%p= t('.intro')
%p= link_to t('.link'), edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)
%p= t('.ignore')
%h3= t('.closure')
%p= t('.change_info')
