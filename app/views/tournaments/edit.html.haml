- model_class = Tournament

- if @tournament.state == 'in-preparation'
  .page-header
    %h1=t '.title', :default => t('helpers.titles.edit', :model => model_class.model_name.human, :default => "Edit #{model_class.model_name.human}")
  = render :partial => "form"
- elsif @tournament.state == 'running'
  .page-header
    %h1= @tournament.title
  = render partial: 'result_editor'